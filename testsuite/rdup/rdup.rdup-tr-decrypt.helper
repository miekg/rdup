#!/bin/bash

# do we have encryption
if cat /dev/null | ./rdup-tr -X<(echo Boe) -c; then : else exit 0; fi

# shoud give zero exit code
./rdup /dev/null testsuite | ./rdup-tr -X<(echo Boe) | \
./rdup-tr -Y<(echo Boe) | ./rdup-up -T > /dev/null
