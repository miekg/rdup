#!/bin/bash

touch sh/.nobackup
total=$(./rdup -nv /dev/null sh 2> /dev/null | sed -s "s/.*sh/sh/g" | grep 'sh/' | wc -l)
# total should be more than 1
if [ $total -lt 2 ];then
    echo FOUT
fi
rm sh/.nobackup
