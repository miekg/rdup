# Defaults for rdup initscript
# sourced by /etc/init.d/rdup
# installed at /etc/default/rdup by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS=""
