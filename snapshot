
copy the directory: cp -lrp dir dir2

run rdup with the snapshot utility:

 - -> file remove the link
   -> link remove the lin
   -> dir, recursively remove

 + -> file
      link
      dir

      typechange 
      niet van belang
        
 
 + rm the link and put the new data there
 = Keep the rest 
