TODO

rdup:
    update manpage

rdup-tr:
    with tar output hardlinks must come last
    update manpage

rdup-up:
    actual use user and group, now they are discarded
    update manpage
    fix -r 
    make -s and -r work together

rdup-simple:
    adapt to new commands

general:
    add extended attributes? man 5 tar explains how to do it, but it
    is A LOT of work AND it is not portable over unixes...?

tests:
    sha1 test
    crypto tests
    encrypt path test
