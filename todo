TODO:

:install:

:rdup:
re-add extended attributes? man 5 tar explains how to do it
-C option can go? We should use rdup-tr?

:rdup-tr:
*testing* *testing* *testing*

opt_null ala rdup
test it with different file types, symlnks hardlinks etc.
overflow in entry-tr for reading rdup entry?
add support for symlinks

extra field in r_entry to hold the pointer for symlinks?

:doc:

:misc:
