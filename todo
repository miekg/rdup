TODO:

rules voor eigen scripts opstellen, waar moet je aan denken bij het
maken van rdup scripts

    check if added or removed
    + or -

    check if the file already exist in the backup directory (maybe
	    another script has hardlinked the prev. backup in here).
    remove it

    put new file in place
    make new directory
    make new sym- or hardlink - if dir exist change permission bits


    en die op miek.nl zetten

rdup-cp fixen, rdup-restore gpg fixen


:install:

:rdup utils:
rdup server pkgs with only rdup-snap and rdup-snap-link??

:rdup:
o encrypted file names?
o -e option
o check extended attributes too? 
    Would be fairly easy?
o -0 also affects the exclude

:tests:
o add conformence test; symlinks etc.

o gpg decryption

o se linux support?
o generic extended attributes. Locally we can use copy
- extended attributes

:gui:
o a high level gui?
o get the gui from sbackup - python based, but works
o has some sort of cron interface...?

:porting:
o port extended attributes to FreeBSD
o cp -plr is gnuis. Don't have time to port this...?
