#!/bin/sh
# This is a shell archive (produced by GNU sharutils 4.2.1).
# To extract the files from this archive, save it to some FILE, remove
# everything before the `!/bin/sh' line above, then type `sh FILE'.
#
# Made on 2006-02-23 09:20 CET by <miekg@elektron>.
# Source directory was `/home/miekg/svn/rdup/trunk/test/004_remote.dir'.
#
# Existing files will *not* be overwritten unless `-c' is specified.
#
# This shar contains:
# length mode       name
# ------ ---------- ------------------------------------------
#    275 -rw------- 004_remote.dsc
#    106 -rw------- 004_remote.help
#    572 -rw------- 004_remote.test
#
save_IFS="${IFS}"
IFS="${IFS}:"
gettext_dir=FAILED
locale_dir=FAILED
first_param="$1"
for dir in $PATH
do
  if test "$gettext_dir" = FAILED && test -f $dir/gettext \
     && ($dir/gettext --version >/dev/null 2>&1)
  then
    set `$dir/gettext --version 2>&1`
    if test "$3" = GNU
    then
      gettext_dir=$dir
    fi
  fi
  if test "$locale_dir" = FAILED && test -f $dir/shar \
     && ($dir/shar --print-text-domain-dir >/dev/null 2>&1)
  then
    locale_dir=`$dir/shar --print-text-domain-dir`
  fi
done
IFS="$save_IFS"
if test "$locale_dir" = FAILED || test "$gettext_dir" = FAILED
then
  echo=echo
else
  TEXTDOMAINDIR=$locale_dir
  export TEXTDOMAINDIR
  TEXTDOMAIN=sharutils
  export TEXTDOMAIN
  echo="$gettext_dir/gettext -s"
fi
if touch -am -t 200112312359.59 $$.touch >/dev/null 2>&1 && test ! -f 200112312359.59 -a -f $$.touch; then
  shar_touch='touch -am -t $1$2$3$4$5$6.$7 "$8"'
elif touch -am 123123592001.59 $$.touch >/dev/null 2>&1 && test ! -f 123123592001.59 -a ! -f 123123592001.5 -a -f $$.touch; then
  shar_touch='touch -am $3$4$5$6$1$2.$7 "$8"'
elif touch -am 1231235901 $$.touch >/dev/null 2>&1 && test ! -f 1231235901 -a -f $$.touch; then
  shar_touch='touch -am $3$4$5$6$2 "$8"'
else
  shar_touch=:
  echo
  $echo 'WARNING: not restoring timestamps.  Consider getting and'
  $echo "installing GNU \`touch', distributed in GNU File Utilities..."
  echo
fi
rm -f 200112312359.59 123123592001.59 123123592001.5 1231235901 $$.touch
#
if mkdir _sh13842; then
  $echo 'x -' 'creating lock directory'
else
  $echo 'failed to create lock directory'
  exit 1
fi
# ============= 004_remote.dsc ==============
if test -f '004_remote.dsc' && test "$first_param" != -c; then
  $echo 'x -' SKIPPING '004_remote.dsc' '(file already exists)'
else
  $echo 'x -' extracting '004_remote.dsc' '(text)'
  sed 's/^X//' << 'SHAR_EOF' > '004_remote.dsc' &&
BaseName: 004_remote
Version: 1.0
Description: Test rdup -c and remote backup
CreationDate: Thu Feb 23 09:12:37 CET 2006
Maintainer: Miek Gieben
Category: 
Component:
CmdDepends: 
Depends: 
Help: 004_remote.help
Pre: 
Post: 
Test: 004_remote.test
AuxFiles: 
Passed:
Failure:
SHAR_EOF
  (set 20 06 02 23 09 14 34 '004_remote.dsc'; eval "$shar_touch") &&
  chmod 0600 '004_remote.dsc' ||
  $echo 'restore of' '004_remote.dsc' 'failed'
  if ( md5sum --help 2>&1 | grep 'sage: md5sum \[' ) >/dev/null 2>&1 \
  && ( md5sum --version 2>&1 | grep -v 'textutils 1.12' ) >/dev/null; then
    md5sum -c << SHAR_EOF >/dev/null 2>&1 \
    || $echo '004_remote.dsc:' 'MD5 check failed'
983288625723aa829bf0b485fe56ac38  004_remote.dsc
SHAR_EOF
  else
    shar_count="`LC_ALL= LC_CTYPE= LANG= wc -c < '004_remote.dsc'`"
    test 275 -eq "$shar_count" ||
    $echo '004_remote.dsc:' 'original size' '275,' 'current size' "$shar_count!"
  fi
fi
# ============= 004_remote.help ==============
if test -f '004_remote.help' && test "$first_param" != -c; then
  $echo 'x -' SKIPPING '004_remote.help' '(file already exists)'
else
  $echo 'x -' extracting '004_remote.help' '(text)'
  sed 's/^X//' << 'SHAR_EOF' > '004_remote.help' &&
No arguments are needed for this test.
This test needs ssh access, so it can probably not
be automated :(
SHAR_EOF
  (set 20 06 02 23 09 15 02 '004_remote.help'; eval "$shar_touch") &&
  chmod 0600 '004_remote.help' ||
  $echo 'restore of' '004_remote.help' 'failed'
  if ( md5sum --help 2>&1 | grep 'sage: md5sum \[' ) >/dev/null 2>&1 \
  && ( md5sum --version 2>&1 | grep -v 'textutils 1.12' ) >/dev/null; then
    md5sum -c << SHAR_EOF >/dev/null 2>&1 \
    || $echo '004_remote.help:' 'MD5 check failed'
316d98a4f5945203e734dad353bc2c3f  004_remote.help
SHAR_EOF
  else
    shar_count="`LC_ALL= LC_CTYPE= LANG= wc -c < '004_remote.help'`"
    test 106 -eq "$shar_count" ||
    $echo '004_remote.help:' 'original size' '106,' 'current size' "$shar_count!"
  fi
fi
# ============= 004_remote.test ==============
if test -f '004_remote.test' && test "$first_param" != -c; then
  $echo 'x -' SKIPPING '004_remote.test' '(file already exists)'
else
  $echo 'x -' extracting '004_remote.test' '(text)'
  sed 's/^X//' << 'SHAR_EOF' > '004_remote.test' &&
# #-- 004_remote.test --#
# source the master var file when it's there
[ -f ../.tpkg.var.master ] && source ../.tpkg.var.master
# use .tpkg.var.test for in test variable passing
[ -f .tpkg.var.test ] && source .tpkg.var.test
# svnserve resets the path, you may need to adjust it, like this:
PATH=$PATH:/usr/sbin:/sbin:/usr/local/bin:/usr/local/sbin:.
X
rm -rf /tmp/storage/REMOTE-TEST
X../../rdup -c HERE . | \
ssh miekg@localhost ~/svn/rdup/trunk/mirror.sh -c -b /tmp/storage/REMOTE-TEST
X
# we should see our directory back
ls -R /tmp/storage/REMOTE-TEST | grep 004_remote
SHAR_EOF
  (set 20 06 02 23 09 20 10 '004_remote.test'; eval "$shar_touch") &&
  chmod 0600 '004_remote.test' ||
  $echo 'restore of' '004_remote.test' 'failed'
  if ( md5sum --help 2>&1 | grep 'sage: md5sum \[' ) >/dev/null 2>&1 \
  && ( md5sum --version 2>&1 | grep -v 'textutils 1.12' ) >/dev/null; then
    md5sum -c << SHAR_EOF >/dev/null 2>&1 \
    || $echo '004_remote.test:' 'MD5 check failed'
de6d66d85361eae5a3bb286406b3fa99  004_remote.test
SHAR_EOF
  else
    shar_count="`LC_ALL= LC_CTYPE= LANG= wc -c < '004_remote.test'`"
    test 572 -eq "$shar_count" ||
    $echo '004_remote.test:' 'original size' '572,' 'current size' "$shar_count!"
  fi
fi
rm -fr _sh13842
exit 0
