########################################
# cron file of rdup                    #
########################################
 
# --- dump - each new month a full dump--- #
10 02 * * * root /usr/sbin/dump.sh -b /vol/backup/$HOSTNAME -b /vol/raid/backup/$HOSTNAME /etc /home /tftpboot
00 02 * * * root /usr/sbin/dump.sh -b /vol/backup/$HOSTNAME -b /vol/raid/backup/$HOSTNAME /var/mail /var/svn /var/lib
