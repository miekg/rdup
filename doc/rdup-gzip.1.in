'\" t
.TH RDUP-GZIP 1 "27 Feb 2006" "@PACKAGE_VERSION@" "@PACKAGE_NAME@ utils"
        
.SH NAME
rdup-gzip, compress \-  compress files received from \fBrdup -c\fR

.SH SYNOPSIS
.B rdup-gzip
[
.IR OPTIONS
]

.SH DESCRIPTION
\fBrdup-gzip\fR compresses the data it receives from \fBrdup\fR with gzip.
.PP
For example,
.PP
        rdup -c list ~/Documents | rdup-gzip | rdup-mirror -c -b /tmp/storage
.PP
Note the \fI-c\fR option for \fBrdup-mirror\fR.

.SH OPTIONS
.TP
.B \-c
This option is ignored, \fBrdup-gzip\fR always works on the files'
content. This "feature" was added because all rdup-util scripts have
such an option.
.TP
.B \-d
Decompress the files.
.TP
.B \-V
Show the version.

.SH SEE ALSO
rdup-crypt(1) and rdup-mirror(8).
