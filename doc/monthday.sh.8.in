'\" t
.TH MONTHDAY.SH 1 "2 Jan 2006" "@PACKAGE_VERSION@" "@PACKAGE_NAME@ utils"

.SH NAME
monthday.sh \- select files from the backup

.SH SYNOPSIS
.B monthday.sh
[
.IR OPTIONS
]
[
.IR +DAY
]

.SH DESCRIPTION
\fBmonthday.sh\fR does not restore anything. It only "greps" the correct
files from the backup (or any other directory).
.PP
The \fI+DAY\fR is used as follows:
EXPLANATION

.SH OPTIONS
.TP
.B \-h 
Provide a small help message.

.SH SEE ALSO
diff(1), hist.sh(1), yesterday(1) and rdup(8)
