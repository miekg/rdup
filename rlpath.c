#include "rdup.h"

/* For each pathname remove references to /../ and /./
 * and remove extranous //
 * Return the sanitized string
 * See man realpath(3)
 */

char * rlpath(char *path) {


    return NULL;
}
